const Loading = () => {
  return (
    <div className='flex flex-1 justify-center items-center h-[96vh]'>
          <h2 className="text-black font-black font-sans text-4xl animate-bounce">pleace wait loading <span className='animate-ping'>...</span></h2>
    </div>
  );
};

export default Loading;
